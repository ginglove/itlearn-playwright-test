{
  "name": "final-exam-playwright",
  "version": "1.0.0",
  "description": "Playwright Automation Testing Suite",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:customer": "playwright test tests/customerFlow.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1"
  }
}